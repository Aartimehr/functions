#include<bits/stdc++.h>
using namespace std;
int main()
{   int a;
    cin>>a;
    int fact =1;
       for (int i=1;i<=a;i++)
       {
           fact=fact*1;
          cout<<i<<endl;
       }
    return 0;
}
